<div class="container text-center">
  <h1 class="form-heading">What is your last name?</h1>
  <%= form_for @user, url: wizard_path, method: :put do |f| %>
    <%= f.text_field :last_name %>
    <%= f.submit "Submit", class: "btn btn-primary" %>
  <% end %>
</div>
